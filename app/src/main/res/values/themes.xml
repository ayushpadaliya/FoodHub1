<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.FoodHub" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.FoodHub" parent="Base.Theme.FoodHub" >
        <item name="colorAccent">@color/white</item>
        <item name="colorPrimary">@color/MainColor</item>
    </style>
    <style name="rating" parent="Base.Theme.FoodHub">
        <item name="android:progressDrawable">@drawable/custom_rating_bar</item>
        <item name="android:minHeight">15dip</item>
        <item name="android:maxHeight">47dip</item>
    </style>
    <style name="Theme.FoodHub1" parent="Base.Theme.FoodHub" >
        <item name="colorAccent">@color/white</item>
        <item name="colorPrimary">@color/white</item>
    </style>
</resources>